{
  "name": "claude-buddy-chat",
  "version": "1.0.0",
  "description": "One-command presence + chat bridge between friends, backed by a private Convex Cloud project",
  "bin": {
    "claude-buddy-chat": "./setup.js"
  },
  "license": "MIT",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "convex": "^1.42.3"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "convex-test": "^0.0.54",
    "vitest": "^4.1.10"
  }
}
